# 交互约定

业务依据：用户要求二周目新主线、支线与易错过内容，非全收集；资料版本与原始校验值见 `docs/source-provenance.json`。本地状态不是游戏实际进度，不能自动判断用户是否已错过某任务。网站不会自动勾选前置或决定游戏分支。

## 统一组件与所有权

| 能力           | 统一实现                     | 变体与验证                                                      |
| -------------- | ---------------------------- | --------------------------------------------------------------- |
| 任务与进度     | TaskCard / lib/progress.js   | 待办、完成、跳过；单元＋浏览器测试                              |
| 对话框         | AppDialog                    | 原文、目录、行囊、确认；原生 dialog 的焦点、Escape 与背景 inert |
| Select/Listbox | 原文的原生 select            | 明确接受操作系统弹出层；无自绘选项列表                          |
| 表单           | App 内的搜索、笔记、文件输入 | 有可见/读屏标签；中文输入完成后提交                             |
| Scrollbar      | src/style.css 全局 token     | 桌面、窄屏、forced-colors                                       |
| Toast          | App notify/live region       | 常规确认；关键错误仍须保留内联提示                              |
| 数据生命周期   | composables/useProgress.ts   | 本地版本化备份；确认替换、确认重置                              |

## 导航与过滤

使用 hash 保存章节、视图、已提交搜索和只看待办。刷新、浏览器前后导航应能恢复；非法章号退回有效章节。没有 history 路由 404 配置要求。章节切换记忆最后位置，搜索结果可以回到正文并短暂高亮任务。手机目录与桌面使用同一数据。

本地搜索在中文输入法 compositionend 后生效；清空立即完成并还原焦点。搜索覆盖当前所选分支及顺路见闻开关范围，空结果有清空操作。分支切换只影响显示，不删除其它分支状态。

进度分母为选中分支与见闻范围，而不是当前搜索结果。跳过单独计数，不算完成；待办过滤同时隐藏完成和跳过。使用单章渲染，搜索/风险视图至多渲染有限静态目录中的条目，不做无限加载。

## 存储、备份与恢复

没有服务端、登录或分析请求；localStorage 每个来源独立。状态 schema=1，稳定任务 ID；未知新旧 ID 导入时过滤并告知。限 512KiB 导入文件、每章 10000 字笔记；验证类型、产品标识、版本和路由。导入先预览再确认替换；取消不修改数据。单个任务只能记录为完成或跳过之一。

存储损坏时保留原始字节，不以默认进度覆盖，可导出原始内容。写入失败保留内存变化并展示常驻警告；用户可导出。跨标签页变动立即暂停旧页写入，必须明确加载另一页或确认以本页覆盖。不要默默覆盖。

重置清空所有分支、札记和设置，初始焦点放在取消，提供先备份操作。不为普通单条勾选弹确认。可逆的跳过提供恢复入口。

## 原文与失败

原文按需加载；显示加载状态、请求失败文字和重新读取按钮。读取失败不得显示空白冒充没有内容。原文允许剧透，与整理路线明确区分；保留行号及定位，不将文本作为 HTML 插入。世界地图本地加载，其余表格明确说明未收录。

## 可访问性与验证

全部控件使用 button/link/input 等原生语义，纯图标操作有中文名字。modal 有标题、焦点约束、Escape 和关闭后焦点恢复；确认从安全按钮开始。交互依赖点击而非悬浮；手机保留同等能力。全局 reduced-motion、forced-colors 与打印样式不影响业务状态。

`tests/browser.py` 正常模式必须通过真实 HTTP 子路径、浏览器重载、下载、多标签页，以及 320/390/768/1024/1440px。禁止浏览器导航的开发容器不能提供真实 HTTP 通过证据，改由 GitHub Actions 执行。axe 自动化通过不等于完整 WCAG 认证，仍需人工视觉与游戏内版本核对。
